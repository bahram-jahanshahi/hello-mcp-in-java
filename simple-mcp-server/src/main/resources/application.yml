spring:
  main:
    banner-mode: off
  application:
    name: simple-mcp-server
  ai:
    mcp:
      server:
        name: simple-mcp-server
        version: 0.1.0
        type: sync
        stdio: true
    
